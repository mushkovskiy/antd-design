# [Dashboard]

> **Цель**: Контроль выполнения текущих ремонтов.

---

## 📌 Общее описание
Компонент называется Dashboard, основное его назначение просмотр состояния текущих ремонтов.
Компонент состоит из двух блоков:
1. Блок фильтрации
Блок фильтрации состоит из следующих antd design компонентов:
Input, placehoder - Введите адрес или номер заявки.
Dropdown, выбор года, опции этого компонента это текущий год и следующий.
Dropdown, выбор квартала, данные для дропдауна Q1-Q4, то есть первый - четвертый квартал.
DropDown, выбор заказчика, данные для дропдауна ВСП,VIP ВСП, ЦУНДО / КИЦ, АЗ,Блок Т., данные тут это данные для отображения, то есть label, value пока моковые сделай.
Логику фильтрации пока не реализовывай.

2. Таблица.
Компонент таблица это (<Table/>) из библиотеки antd design.
Структура табилцы:
Таблица состоит из двух колонок
1. Террритория
2. Планирование ремонта.

3. Тестовые данные для рендера таблицы возьми в файле data-example.json.

---
## Описание структуры таблицы
Колонка 'Территория' должна иметь должна иметь кастомный заголовок, по мимо текста Территория, должен быть текст 'потребности в шт.,бюджет в рублях без НДС'
Колонка 'Планирование ремонта' имеет многоуровневый заголовок. Главный заголовок 'Планирование ремонта', подзаголовки:
Потребность,Стоимость,Бюджет,Бюджет.
Нужно убрать видимое разделение между дочерними колонками внутри группы.
Данные который рендерятся в строках таблицы имеют древовидную структуру.



## Некоторые ключи данных и их смысл.
В объектах данных будут в  том числе будут следующие ключи:
aptrBudgetGosbs - Список ГОСБов, можно рассматривать как подроазделения.
aptrBudgetObjects - Список объектов определенного ГОСБа
aptrProjects - список потребностей.

## Описание тестовых данных и рендера таблицы
В поле content, объекты, это данные для рендера строк.
Каждая строка имеет древовидную стуктуру.
Ниже описание 'верхнеуровневой' строки.
Исходные данные это объект со ключом context, значение которого массив объектов, каждый объект это верхнеуровневая строка, еще будем называеть такой верхнеуровневый отбъект как территория.
В колонке 'Территория' название строки - поле tbId.
В колонке Планирование ремонта, значение для подзаголовка 'Потребность' определяется следующим образом:
в каждой территории есть поле aptrBudgetGosbs,это массив, в каждом объекте массива есть поле aptrBudgetObjects, это поле тоже массив, в объектах уже этого массива есть поле aptrProjects, и это тоже массив.
Нужно собрать количество элементов этого массива по всей стуркутре для очередной территории это и будет значение для подзаголовка 'Потребность'
В колонке Планирование ремонта,  для подзаголовков Стоимость и Бюджет нужно будет нарисовать общий для этих двух подзаголовков компонент, по смыслу похожий на ProgressBar.
С точки зрения верстки, ProgressBar это div, высотой 30px,и шириной 265px. и border-radius 16px
Логика заполнения 'ProgressBar':
в начале ProgressBar указана Стоимость, она определятется суммированием значений поля summ при условии что в этом же объекте - потребности, значение поля status === INCLUDED_AP, просуммировать нужно для всех потребностей для каждой территории.
В конце ProgressBar указан Бюджет, его значение берем из объекта территории в поле planBudget.
Далее, нужно добавить сам прогресс, то есть если значение Стоимости состовляет например 50% от всего бюджета, то заполняем катосмный ProgressBar на половину, таким цветом rgb(171, 169, 241), оставшуюся часть заполняем цветом rgb(233, 234, 255)
Если Стоимость превышает бюджет то весь ProgressBar rgb(255, 120, 117), если равна бюджету то rgb(81, 158, 65)
В колонке Планирование ремонта,  для подзаголовка Отклонение, значение определятеся как разность бюджета и стоимости, для каждой территории. Если разница отрицательная, то ставим минус перес резульататом, если разница 0, то рисуем вместо значения иконку <CheckOutlined />

## 🧠 Ожидаемая логика работы

Опиши пошагово, как должен работать компонент:
- Какие действия пользователь может совершить?
- Как компонент реагирует на изменения входных данных?
- Есть ли внутреннее состояние? Как оно управляется?
- Какие побочные эффекты (запросы, аналитика и т.д.) должны происходить?

Пример:
- При монтировании компонента загружается список элементов через `useQuery`.
- Пользователь может ввести текст в поле поиска — это вызывает клиентскую фильтрацию.
- При выборе элемента из списка вызывается колбэк `onSelect`.

---

## 📥 Props (входные параметры)

Укажи все пропсы с типами (если используешь TypeScript) и описанием:

```ts
interface ComponentProps {
  /** Описание пропса */
  items: Item[];
  /** Колбэк при выборе элемента */
  onSelect?: (item: Item) => void;
  /** Флаг для отображения загрузки */
  isLoading?: boolean;
}
